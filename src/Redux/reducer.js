const initialState = {
    launches: [],
    loading: false,
    error: null
};

const launchesReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default launchesReducer;